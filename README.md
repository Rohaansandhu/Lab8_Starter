# Lab8-Starter

# Explore question
Graceful degradation means building an app to still work even when something goes wrong, like losing internet connection. Service workers support this by caching network requests so the app can fall back to locally stored resources when offline. In this lab, even without internet, the service worker serves the cached recipe data and page assets so the app still functions.